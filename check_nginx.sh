#!/bin/bash

###############################################################################
# 检测 nginx 进程是否存在
###############################################################################

A=`ps -C nginx --no-header |wc -l`
if [ $A -eq 0 ];then
    # echo nonexist
    exit 1
else
    # echo exist
    exit 0
fi